Python code:
```
    def maxSodas(N):
        if N <= 0: # edge case
            return 0

        total = N # initialize counter
        while N > 2:
            newBottles = N//3
            total += newBottles
            N = (N % 3) + new_bottles # Calculate how many bottles you'll have left after returning 

        if N == 2:
            total += 1 # Borrow a bottle, drink, and return

        return total

    # Example
    N = 11
    print(f"Maximum number of CalamanSodas from {N} bottles: {maxSodas(N)}")
```